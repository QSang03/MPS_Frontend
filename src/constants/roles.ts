/**
 * User roles in the MPS system
 * Backend has diverse roles, so we use string type instead of enum
 */
// export enum UserRole {
//   SYSTEM_ADMIN = 'SystemAdmin',
//   CUSTOMER_ADMIN = 'CustomerAdmin',
//   USER = 'User',
// }

export type UserRole = string
export type UserRoleType = string
